<nav class="navbar navbar-toggleable-md navbar-inverse bg-inverse navbar-full">
    
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
    
  <a class="navbar-brand" href="#">Localee</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    
        <li class="nav-item">
          <a class="nav-link" href="#HowItWorks">Campaigns</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#Advertisers">Profile</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#Advertisers">Reports</a>
        </li>       
    </ul>
      
     <ul class="nav navbar-nav pull-xs-right">
      <% if user_signed_in? %>
            <li class="nav-item pull-lg-right">
          <%= link_to('Log out', destroy_user_session_path, :method => :delete, :class => "nav-link") %>
      </li>
      <% else %>          
    
    <li class="nav-item pull-lg-right">
                <%= link_to "Log in", new_user_session_path, :class => "nav-link" %>
      </li>
        <% end %>

    </ul>  

  </div>
</nav>
  
<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">
    <div class="col-sm-6">
        <h1>Welcome, User</h1>
    </div>

    <div class="col-sm-6 pull-sm-right">
        <%= link_to 'New Campaign', new_campaign_path, :class=>  "btn btn-primary btn-sm" %>
            <div class="dropdown">
              <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenu2" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Dropdown
              </button>
              <div class="dropdown-menu" aria-labelledby="dropdownMenu2">
                <button class="dropdown-item" type="button">Action</button>
                <button class="dropdown-item" type="button">Another action</button>
                <button class="dropdown-item" type="button">Something else here</button>
              </div>
            </div>
    </div>
  </div>
</div>




<table>
  <thead>
    <tr>      
      <th>Title</th>
        <th>File</th>
      <th>Start date</th>
      <th>End date</th>
      <th>Theaters</th>
      <th>User Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @campaigns.each do |campaign| %>
      <tr>        
        <td><%= campaign.title %></td>
          <tr>    
        <td><%= campaign.campaign_file %></td>
              <tr>    
        <td><%= campaign.start_date %></td>
        <td><%= campaign.end_date %></td>
        <td><%= campaign.theaters %></td>
        <td><%= campaign.user.email %></td>
        <td><%= link_to 'Show', campaign %></td>
          <% if user_signed_in? %>
        <td><%= link_to 'Edit', edit_campaign_path(campaign) %></td>
        <td><%= link_to 'Destroy', campaign, method: :delete, data: { confirm: 'Are you sure?' } %>
            <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


